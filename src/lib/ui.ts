// Shared button treatments, extracted from the homepage build so every page
// uses the exact same look rather than re-deriving the class strings.
export const btnPrimary =
  'inline-flex w-full min-h-[52px] items-center justify-center rounded-lg bg-grad-accent px-6 py-3 font-medium text-navy transition-[filter] duration-200 hover:brightness-105 sm:w-auto';

export const btnSecondary =
  'inline-flex w-full min-h-[52px] items-center justify-center rounded-lg border border-teal px-6 py-3 font-medium text-teal transition-colors duration-200 hover:bg-teal/10 sm:w-auto';

export const btnOnAccent =
  'inline-flex w-full min-h-[52px] items-center justify-center rounded-lg bg-navy px-6 py-3 font-medium text-white transition-[filter] duration-200 hover:brightness-110 sm:w-auto';
