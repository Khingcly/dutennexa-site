# Server-side (Vercel serverless function for the AI advisor)
ANTHROPIC_API_KEY=

# Client-exposed (PUBLIC_ prefix required by Astro to reach the browser bundle).
# Contact form posts here; falls back to a mailto: link if unset or the POST fails.
PUBLIC_CONTACT_WEBHOOK_URL=
